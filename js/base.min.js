"use strict";
!function () {
    for (var o, e = function () {
    }, t = ["assert", "clear", "count", "debug", "dir", "dirxml", "error", "exception", "group", "groupCollapsed", "groupEnd", "info", "log", "markTimeline", "profile", "profileEnd", "table", "time", "timeEnd", "timeStamp", "trace", "warn"], i = t.length, r = window.console = window.console || {}; i--;) r[o = t[i]] || (r[o] = e)
}(), function () {
    var o = document.querySelector(".burger-container"), e = document.querySelector(".header");
    o.onclick = function () {
        e.classList.toggle("menu-opened")
    }
}(), $(window).on("resize", function () {
    $(".slick-track").innerHeight();
    return !1
});
var $root = $("html, body");
$("a.s-scroll").on("click", function () {
    var o = $.attr(this, "href");
    return $root.animate({scrollTop: $(o).offset().top}, 500, function () {
        window.location.hash = o
    }), !1
});
for (var background = "#ccc", backgroundMask = "rgba(255,255,255,0.92)", backgroundVideoUrl = "none", list = $(".bg-img"), i = 0; i < list.length; i++) {
    var src = list[i].getAttribute("data-image-src");
    list[i].style.backgroundImage = "url('" + src + "')", list[i].style.backgroundRepeat = "no-repeat", list[i].style.backgroundPosition = "center", list[i].style.backgroundSize = "cover"
}
for (list = $(".bg-color"), i = 0; i < list.length; i++) {
    src = list[i].getAttribute("data-bgcolor");
    list[i].style.backgroundColor = src
}
$(function () {
    $(".video-container video, .video-container object").maximage("maxcover")
}), $(window).on("load", function () {
    $("#page-loader").addClass("hidden")
});